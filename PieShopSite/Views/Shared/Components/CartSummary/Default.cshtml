@model CartViewModel

@if (Model.Cart.ShoppingCartItems.Any())
{
    <li>
        <a>
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">
                @Model.Cart.ShoppingCartItems.Count
            </span>
        </a>
    </li>
}
 